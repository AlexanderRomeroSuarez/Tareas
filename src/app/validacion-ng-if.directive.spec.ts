import { ValidacionNgIfDirective } from './validacion-ng-if.directive';

describe('ValidacionNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
